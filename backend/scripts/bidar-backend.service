# 배포 서버 /etc/systemd/system/bidar-backend.service 에 복사해서 사용
# WorkingDirectory / ExecStart / User는 실제 배포 경로·계정에 맞게 수정
[Unit]
Description=BIDAR FastAPI backend
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/bidar/backend
ExecStart=/opt/bidar/backend/scripts/start.sh
Restart=on-failure
RestartSec=5
User=bidar

[Install]
WantedBy=multi-user.target
